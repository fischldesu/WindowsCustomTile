<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LiveTileWinUI3.Pages.HTTPXmlServer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LiveTileWinUI3.Pages"
    xmlns:Components="using:LiveTileWinUI3.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="42"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" FontSize="32">HTTP Xml server</TextBlock>
        <Grid Grid.Row="1" Margin="0 16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ScrollViewer Padding="16" Margin="0 16" Background="#20808080" CornerRadius="6" BorderThickness="1" BorderBrush="#2b2b2b64">
                <StackPanel x:Name="inputStack" Orientation="Vertical" />
            </ScrollViewer>
            <StackPanel Grid.Column="1" Padding="16" Orientation="Vertical">
                <Button HorizontalAlignment="Stretch" x:Name="newItem" Click="newItem_Click" ToolTipService.ToolTip="New HTTP link input">Append</Button>
                <Button HorizontalAlignment="Stretch" x:Name="randomize" Click="randomize_Click" Margin="0 8" ToolTipService.ToolTip="Randomize entire list">Randomize</Button>
                <Button x:Name="clear" Click="clear_Click" HorizontalAlignment="Stretch" Style="{StaticResource RedButtonStyle}" CornerRadius="4" ToolTipService.ToolTip="Clear all links">Clear</Button>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="2" HorizontalAlignment="Right" Orientation="Horizontal">
            <ComboBox x:Name="updateRecurrence" Height="32" CornerRadius="6 0 0 6" />
            <Button
                Height="32"
                CornerRadius="0 6 6 0"
                Margin="-1 0 0 0"
                x:Name="submit"
                HorizontalAlignment="Right"
                Style="{StaticResource AccentButtonStyle}"
                Click="submit_Click">Submit</Button>
        </StackPanel>
        <InfoBar
           x:Name="info"  
           Grid.Row="0"    Grid.RowSpan="3"
           Grid.Column="0" Grid.ColumnSpan="2"
           VerticalAlignment="Bottom"
           HorizontalAlignment="Center"
           IsClosable="True"   
           IsOpen="False"
           Title="Info bar title"   
           Message="Uninitialized Infobar message"   
           Severity="Warning"
           Canvas.ZIndex="10"
           Margin="0 0 0 16"
           />
    </Grid>
</Page>
